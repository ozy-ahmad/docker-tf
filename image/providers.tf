terraform {
  required_providers {
    docker = {
      source  = "kreuzwerker/docker"
      version = "~> 2.13.0"
    }
  }
}

//Empty provider configuration blocks are not required
#provider "docker" {}
